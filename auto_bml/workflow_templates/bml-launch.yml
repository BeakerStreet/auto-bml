name: BML Launch

on:
  workflow_dispatch:

jobs:
  launch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
    steps:
      - uses: actions/checkout@v4

      - uses: beakerstreet/auto-bml@main
        with:
          command: launch
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          google_ads_developer_token: ${{ secrets.GOOGLE_ADS_DEVELOPER_TOKEN }}
          google_ads_client_id: ${{ secrets.GOOGLE_ADS_CLIENT_ID }}
          google_ads_client_secret: ${{ secrets.GOOGLE_ADS_CLIENT_SECRET }}
          google_ads_refresh_token: ${{ secrets.GOOGLE_ADS_REFRESH_TOKEN }}
          google_ads_customer_id: ${{ secrets.GOOGLE_ADS_CUSTOMER_ID }}
          daily_budget_usd: "20"
          conversion_url: ${{ secrets.CONVERSION_URL }}

      - name: Commit run metadata
        run: |
          git config user.name "auto-bml"
          git config user.email "auto-bml@users.noreply.github.com"
          git add .bml/runs.json
          git diff --cached --quiet || git commit -m "bml: record launch run"
          git pull --rebase
          git push
